<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joyce.restaurantrescue">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PickRestaurant" />
        <activity android:name=".MIT" />
        <activity android:name=".Chilis" />
        <activity android:name=".Menu1" />
        <activity android:name=".Menu2" />
        <activity android:name=".Reserve1" />
        <activity android:name=".Reserve2"></activity>
    </application>

</manifest>